{
    "name": "isp-outage-map",
    "version": "1.0.0",
    "description": "Lightweight web-based map application for ISPs to display service outages",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "preview": "vite preview",
        "deploy": "npm run build && wrangler pages deploy dist"
    },
    "keywords": [
        "mapbox",
        "outage-map",
        "isp",
        "supabase",
        "vanilla-js"
    ],
    "author": "",
    "license": "MIT",
    "devDependencies": {
        "vite": "^5.0.0",
        "terser": "^5.0.0"
    },
    "dependencies": {
        "mapbox-gl": "^3.15.0"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}